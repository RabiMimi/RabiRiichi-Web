/**
 * ResultAnimation3D is intentionally disabled.
 *
 * Agari/tsumo results are handled by CallPrompt (per-player seat flash) and
 * ResultPanel (detailed scoring).
 */
export function ResultAnimation3D(): null {
  return null;
}
