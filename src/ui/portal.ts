import { createRef } from 'react';

export const tooltipPortalTarget = createRef<HTMLElement>();
export const playerOverlayPortalTarget = createRef<HTMLElement>();
export const stickerPortalTarget = createRef<HTMLElement>();
